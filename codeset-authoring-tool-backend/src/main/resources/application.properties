
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
hibernate.show_sql=true



#spring.datasource.url=jdbc:postgresql://localhost:5432/postgres
#spring.datasource.url=jdbc:postgresql://codeset-authoring-tool-db-prod:5432/postgres
spring.datasource.url=${POSTGRES_URL}
spring.datasource.username=${POSTGRES_USER}
spring.datasource.password=${POSTGRES_PASSWORD}

spring.mail.host=${EMAIL_HOST}
spring.mail.port=${SMTP_PORT}
#spring.mail.username=your-email@gmail.com
#spring.mail.password=your-email-password
#spring.mail.properties.mail.smtp.auth=true
#spring.mail.properties.mail.smtp.starttls.enable=true
#spring.mail.properties.mail.smtp.ssl.trust=${EMAIL_HOST}
spring.mail.protocol=${EMAIL_PROTOCOL}

adminDefaultUsername=admin
adminDefaultEmail=${ADMIN_EMAIL}
jwt.secret=${JWT_SECRET}
adminDefaultPassowrd=${ADMIN_USER_PASSWORD}

emailPort=${SMTP_PORT}
emailProtocol=${EMAIL_PROTOCOL}
emailHost=${EMAIL_HOST}
emailFrom=${EMAIL_FROM}
emailSubject="Codeset Authoring Tool Authentication"
frontendUrl=${FRONTEND_URL}

